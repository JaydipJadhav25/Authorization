<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Signup</title>
  <style>
    /* General Reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Arial', sans-serif;
}

body {
    background: linear-gradient(135deg, #6A11CB, #8ca947);
    height: 100vh;
    display: flex;
    justify-content: center;
    align-items: center;
}

.container {
    max-width: 400px;
    width: 90%;
    background: #fff;
    border-radius: 10px;
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
    overflow: hidden;
}

.form-wrapper {
    padding: 20px;
    text-align: center;
}

h1 {
    margin-bottom: 20px;
    font-size: 1.8rem;
    color: #333;
}

.input-group {
    margin-bottom: 15px;
    text-align: left;
}

.input-group label {
    display: block;
    margin-bottom: 5px;
    font-size: 0.9rem;
    color: #555;
}

.input-group input {
    width: 100%;
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 5px;
    font-size: 1rem;
    outline: none;
    transition: 0.3s ease;
}

.input-group input:focus {
    border-color: #2575FC;
    box-shadow: 0 0 5px rgba(37, 117, 252, 0.3);
}

.btn {
    display: inline-block;
    width: 100%;
    padding: 10px;
    background: linear-gradient(135deg, #6A11CB, #2575FC);
    color: #fff;
    font-size: 1rem;
    font-weight: bold;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: 0.3s ease;
}

.btn:hover {
    background: linear-gradient(135deg, #2575FC, #6A11CB);
}

.login-link {
    margin-top: 15px;
    font-size: 0.9rem;
    color: #666;
}

.login-link a {
    color: #2575FC;
    text-decoration: none;
    font-weight: bold;
    transition: 0.3s ease;
}

.login-link a:hover {
    color: #6A11CB;
}
h2{
    text-align: center;
    color: red;
}
  </style>
</head>
<body>
    <div class="container">
        <% if (locals.result) { %>
            <h2><%= locals.result %></h2>
           <% } %>
        <div class="form-wrapper">

           
            <h1>Login</h1>
            <form action="/user/login" method="POST">
             
                <div class="input-group">
                    <label for="email">Email</label>
                    <input type="email" id="email" name="email" required placeholder="Enter your email">
                </div>
                <div class="input-group">
                    <label for="password">Password</label>
                    <input type="password" id="password" name="password" required placeholder="Enter your password">
                </div>
               
                <button type="submit" class="btn">Login Up</button>
            </form>
            <p class="login-link">create a Account <a href="/signup">signup here</a>.</p>
        </div>
    </div>
</body>
</html>
